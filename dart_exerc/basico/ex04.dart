void main() {
  
  // >>>>>Básico<<<<<
  // 4. Entrar via teclado com o valor da cotação do dólar e uma certa quantidade de dólares. Calcular e exibir o valor correspondente em Reais (R$).
  double cotacao_dolar = 5.5;
  double qtd_dolares = 100;
  double valor_em_reais = cotacao_dolar * qtd_dolares;
  
  print("O valor correspondente em reais é R\$$valor_em_reais");
  
}